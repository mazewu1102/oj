#include <cstdio>
#include <cstdlib>
#include <cstring>
#include <iostream>

using namespace std;

int main()
{
    char s[81];
    scanf("%s", s);
    int len = strlen("s");
    int l = m = r = len / 3;
    if (len%3 == 1)
        m--;
    for (int i = 0; i < ; ++i)
    {
        printf("%c", );
        for (int j = 0; j < m; ++j)
            printf(" ");
    }
    for (int i = 0; i < middle; ++i)
        print("%c", s[i])
    for (int i = 0; i < right; ++i)
        print("%c", s[i])
    return 0;
}
